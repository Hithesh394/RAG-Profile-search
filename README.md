# RAG-Profile-search
A semantic search web app built using Streamlit, FAISS, and Sentence Transformers to find the most relevant candidate profiles based on natural language queries. It demonstrates a Retrieval-Augmented Generation (RAG) workflow for AI-powered profile matching and talent discovery.
